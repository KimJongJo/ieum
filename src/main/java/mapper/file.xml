<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.file">
	<select id="selectFileByuNo" parameterType="int" resultType="file">
        select file_no, file_name, file_path, file_category
        from file
        where file_no = #{fileNo} 
    </select>



	<insert id ="insertFile" parameterType="file" useGeneratedKeys="true" keyProperty="fileNo">
		INSERT INTO file (file_no, file_name, file_path, file_category)
        VALUES(#{fileNo}, #{fileName}, #{filePath},#{fileCategory})
	</insert>
	
	<update id="updateFile" parameterType="file">
		update file set 
			file_name = #{fileName},
            file_path = #{filePath},
            file_category = #{fileCategory}
        where file_no = #{fileNo}
	</update>
</mapper>	