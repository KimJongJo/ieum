<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.notice">
	<insert id="insertNotice" parameterType="notice">
		INSERT INTO notice
		(title, content, is_pinned, file_no, u_no) VALUES (#{title},
		#{content}, #{isPinned}, #{fileNo, jdbcType=INTEGER}, #{uNo})
	</insert>
	<select id="selectNoticeList" parameterType="map"
		resultType="notice">
		SELECT *
		FROM notice n
		LEFT JOIN file f
		using(file_no)
		WHERE n.is_pinned = #{isPinned}
		<if test="keyword != null and keyword != ''">
			AND (n.title LIKE CONCAT('%', #{keyword}, '%')
			OR n.content
			LIKE CONCAT('%', #{keyword}, '%'))
		</if>

		<choose>
			<when test="sort == 'title'">ORDER BY n.title DESC</when>
			<otherwise>ORDER BY  n.n_created DESC</otherwise>
		</choose>
		<choose>
			<when test="isPinned == 1">LIMIT 5</when>
			<otherwise>LIMIT #{row}, 10</otherwise>
		</choose>
	</select>
	<select id="selectNoticeDetail" resultType="notice">
		SELECT * FROM notice
		WHERE n_no = #{nNo}
	</select>
	<select id="selectNoticeCnt" resultType="Integer">
		SELECT COUNT(*) FROM
		notice WHERE is_pinned = 0;
	</select>
	<update id="updateNotice" parameterType="notice">
		UPDATE notice SET
		title=#{title}, content=#{content}, is_pinned=#{isPinned},
		n_updated=now(), file_no=#{fileNo}
		WHERE n_no=#{nNo}
	</update>
	<delete id="delNotice" parameterType="Integer">
		DELETE FROM notice WHERE
		n_no=#{nNo}
	</delete>
	<select id="selectUserNm" parameterType="Integer"
		resultType="String">
		SELECT username FROM member WHERE u_no = #{uNo}
	</select>
</mapper>	