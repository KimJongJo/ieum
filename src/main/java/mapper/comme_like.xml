<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.comme_like">
	<!-- 공감 상태 확인-->
    <!-- <select id="selectCommeEmpathy" parameterType="int" resultType="int">
    	select count(*)
    	from comme_like
    	where u_no = #{uNo} and comme_no = #{commeNo}
    </select> -->
    
    <!-- 2. 좋아요 추가 -->
    <!-- <insert id="insertCommeEmpathy" parameterType="map" useGeneratedKeys="true" keyProperty="commuLikeNo">
        insert into comme_like (u_no, comme_no)
        values (#{uNo}, #{commeNo})
    </insert> -->
    
    <!-- 공감 삭제 -->
    <!-- <delete id="deleteCommeEmpathy" parameterType="map">
    	delete from comme_like
    	where u_no = #{uNo} and comme_no = #{commuNo}
    </delete>
 -->    
    <!-- community의 empathy update -->
    <!-- <update id="updateCommentEmpathy" parameterType="map">
        UPDATE community
        SET empathy = empathy + #{delta}
        WHERE comme_no = #{commeNo}
    </update>
     -->
    <!-- 커뮤니티 공감수 가져오기 -->
    <!-- <select id="selectCommentEmpathy" parameterType="int" resultType="int">
	    select empathy from community WHERE comme_no = #{commeNo}
	</select> -->
</mapper>	